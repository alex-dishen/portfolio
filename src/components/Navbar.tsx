import { StyledNavbar, ImageHolder } from 'components/styles';

function Navbar() {
  return (
    <StyledNavbar>
      <ImageHolder to="/" />
      <ImageHolder to="/" />
      <ImageHolder to="/" />
      <ImageHolder to="/" />
      <ImageHolder to="/" />
    </StyledNavbar>
  );
}

export default Navbar;
